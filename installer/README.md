Windows packaging: a PowerShell installer first, WiX/MSI packaging after it.

See docs/installer-and-updates.md for the install layout, ACLs, service
registration properties, and the rule that enrollment tokens are never passed
as command-line arguments.

Not present yet.
